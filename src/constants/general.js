export const DEVELOPMENT_ENV = "development";

export const ACTION_SOURCE = {
    USER: "USER", // Actions initiated by the user
    SYSTEM: "SYSTEM", // Actions initiated by the system (e.g. side-effect actions)
}


export const FLAGS = {
    ENABLE_ESIGN: "FLAG_ENABLE_ESIGN"
};
